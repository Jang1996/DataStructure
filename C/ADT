ADT 线性表（List)
Data线性表的数据对象是集合为{a1,a2....an}的表(下标从1开始)，其数据类型为DataType。其中，除第一个元素外，其它元素有且仅有一个直接前驱元素，除最后一个
元素外，其它元素有且仅有一个直接后继元素。数据与元素之间关系是一一对应关系。
Operation
InitList(  *L);初始化线性表
ListEmpty(  *L) ;
GetElem( L,i,*e);
LocateElem(L,*e);
ListLength( *L);
ListInsert( *L,i,*e);
ListDelete(*L,i,*e);
ClearList(*L);
endADT

